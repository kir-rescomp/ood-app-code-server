---
batch_connect:
  template: "basic"

script:
  native:
    - "--ntasks=1"
    - "--mem=<%= ram %>G"
    - "--cpus-per-task=<%= cores %>"<% if auto_queues.start_with?("gpu") %>
    - "--account"
    - "<%= gpu_account %>"
    - "--gpus-per-task"
    - "1"<% end %>
